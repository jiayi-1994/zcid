server:
  port: "8080"
  log_level: info

database:
  host: localhost
  port: 5432
  name: zcid
  user: zcid
  password: zcid_dev
  ssl_mode: disable

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

minio:
  endpoint: localhost:9000
  access_key: zcid
  secret_key: zcid_dev_key
  use_ssl: false

auth:
  jwt_secret: dev-jwt-secret
