server:
  port: "8080"
  log_level: "info"

database:
  host: "localhost"
  port: 5432
  name: "zcid"
  user: "zcid"
  password: "password"
  ssl_mode: "disable"

redis:
  host: "localhost"
  port: 6379
  password: ""  # Use REDIS_PASSWORD env var instead
  db: 0

minio:
  endpoint: "localhost:9000"
  access_key: "minioadmin"
  secret_key: "minioadmin"
  use_ssl: false

auth:
  jwt_secret: "dev-jwt-secret-key-for-local"

notification:
  slack_base_url: "http://localhost:5173"

step_executions:
  retention_days: 90
