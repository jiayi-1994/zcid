apiVersion: v1
kind: Service
metadata:
  name: {{ include "zcid.fullname" . }}
  labels:
    {{- include "zcid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zcid.selectorLabels" . | nindent 4 }}
